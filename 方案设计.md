# 方案设计

## 一、功能需求分析

### 1.1 用户角色

- **运维人员**：主要使用者，通过系统查看设备状态、获取故障预警和维护建议。
- **系统管理员**：负责模型训练、数据管理和系统配置。

### 1.2 功能需求

| 功能模块 | 需求描述 | 优先级 |
|---------|---------|:---:|
| 数据上传 | 支持上传 CSV 格式的传感器批量数据，也支持单条数据手动输入。 | 高 |
| 状态仪表盘 | 实时展示设备健康度评分、故障概率、关键参数趋势图。 | 高 |
| 故障预测 | 输入设备参数，一键获取故障预测结果、置信度和运维建议。 | 高 |
| 历史记录 | 查看历史预测记录，支持按时间和结果类型筛选。 | 中 |
| 模型管理 | 后台支持模型训练、评估和更新（预留功能）。 | 低 |

## 二、方案论证

### 2.1 技术选型依据

| 技术环节 | 选型方案 | 选型理由 |
|---------|---------|---------|
| 分类算法 | 随机森林 | 抗过拟合、对异常值不敏感、适配高维非线性数据，在工业预测场景中应用广泛。 |
| 优化算法 | 遗传算法 | 具有全局搜索能力，适用于复杂超参数空间的自动寻优。 |
| Web 框架（后端） | Flask | 轻量级、易于部署、与 Python 机器学习生态无缝集成。 |
| 前端 UI | HTML5 + Bootstrap + Chart.js | 响应式布局、图表可视化、无需额外编译工具链。 |
| 数据库 | SQLite | 轻量级嵌入式数据库，无需独立部署，适合课程项目规模。 |
| 开发语言 | Python 3.10+ | 统一的算法开发与后端服务语言，降低技术栈复杂度。 |

### 2.2 可行性分析

- **数据可行性**：采用 UCI 公开数据集 AI4I 2020 Predictive Maintenance Dataset，10,000 条样本、14 个字段、无缺失值，专为预测性维护研究设计，下载即用。
- **技术可行性**：随机森林、遗传算法、Flask 均为成熟技术，文档丰富，社区活跃，开发难度可控。
- **时间可行性**：采用模块化开发策略，7-10 天可完成从数据处理到系统集成的全部工作。

## 三、方案规划

### 3.1 技术栈

| 层级 | 技术选型 |
|------|---------|
| **前端** | HTML5 + CSS3 + Bootstrap 5 + Chart.js + JavaScript (Fetch API) |
| **后端** | Python 3.10 + Flask + Flask-CORS |
| **算法** | scikit-learn（随机森林）、DEAP（遗传算法）、pandas / numpy（数据处理）、imbalanced-learn（SMOTE） |
| **数据库** | SQLite 3 + SQLAlchemy（可选 ORM） |
| **开发工具** | VSCode / Jupyter Notebook + Git |
| **部署** | 本地运行（Flask 内置服务器），可选 Docker 容器化 |

### 3.2 需配置的外部资源

| 资源类型 | 具体内容 | 获取方式 |
|---------|---------|---------|
| 数据集 | AI4I 2020 Predictive Maintenance Dataset（ai4i2020.csv） | UCI 官方下载：https://archive.ics.uci.edu/ml/machine-learning-databases/00601/ai4i2020.csv |
| Python 包 | scikit-learn、DEAP、pandas、numpy、imbalanced-learn、Flask、Flask-CORS | pip install |
| 前端库 | Bootstrap 5、Chart.js | CDN 引入 |
| 参考代码 | GitHub 预测性维护开源项目 | 参考学习，不直接复用 |

### 3.3 数据集说明

- **名称**：AI4I 2020 Predictive Maintenance Dataset
- **规模**：10,000 条样本，14 个字段，无缺失值
- **特征**：空气温度、工艺温度、转速、扭矩、刀具磨损（连续型 / 整型），产品类型 L / M / H（类别型）
- **标签**：`Machine failure`（是否故障，0/1）及五种故障子类型（TWF / HDF / PWF / OSF / RNF）
- **特点**：故障样本占比约 3.4%，类别不平衡显著；特征间线性相关性低，适合非线性模型

### 3.4 前端 UI 方案

| 页面 | 功能描述 |
|------|---------|
| **首页 / 仪表盘** | 设备健康度评分（环形图 / 仪表盘）、故障概率进度条、关键参数趋势图（折线图）、预警信息列表。 |
| **数据上传页** | 支持拖拽或点击上传 CSV 文件，显示数据预览表格。 |
| **单条预测页** | 表单输入设备参数（温度、转速、扭矩、刀具磨损、产品类型），点击预测后显示结果。 |
| **历史记录页** | 表格展示历史预测记录，支持按时间排序和结果筛选。 |

### 3.5 后端 API 设计

| 接口 | 方法 | 功能 | 请求体 / 参数 |
|------|:---:|------|-------------|
| `/predict` | POST | 单条数据故障预测 | JSON 格式设备参数 |
| `/batch_predict` | POST | 批量数据故障预测 | CSV 文件（multipart/form-data）|
| `/health` | GET | 健康检查 | 无 |
| `/history` | GET | 获取历史预测记录 | 分页参数（可选）|

### 3.6 算法模块设计

**模块一：模式识别——随机森林故障分类**

- 数据预处理：删除 ID 列和故障子类型标签列（防止数据泄露）。
- 特征工程：构造温度差、功率、应变等衍生特征。
- 类别不平衡处理：SMOTE 过采样 + `class_weight='balanced'`。
- 模型训练：RandomForestClassifier，交叉验证评估。

**模块二：智能优化——遗传算法超参数调优**

- 优化目标：最大化交叉验证 F1-Score。
- 优化参数：`n_estimators`（50-300）、`max_depth`（5-30）、`min_samples_split`（2-20）。
- 算法流程：种群初始化 → 适应度评估 → 选择 → 交叉 → 变异 → 迭代进化。

**模块三：知识工程——健康度规则引擎**

基于 AI4I 数据集的实际故障触发条件构建 IF-THEN 规则库：

- 散热风险规则：温差 < 8.6K 且 转速 < 1380rpm → HDF 风险。
- 功率异常规则：功率 < 3500W 或 > 9000W → PWF 风险。
- 过载风险规则：刀具磨损 × 扭矩超过产品类型对应阈值 → OSF 风险。
- 刀具磨损预警规则：刀具磨损在 180-240min 区间 → TWF 风险。
- 综合评分：满分 100 分扣分制，输出健康 / 预警 / 危险三级状态及运维建议。

### 3.7 系统架构图

```text
┌─────────────────────────────────────────────────────────────────┐
│                      前端 UI（Web 浏览器）                      │
│  ┌──────────┐ ┌──────────┐ ┌──────────┐ ┌──────────┐          │
│  │ 数据上传  │ │ 状态仪表盘│ │ 故障预测  │ │ 历史记录  │          │
│  └──────────┘ └──────────┘ └──────────┘ └──────────┘          │
└─────────────────────────────┬───────────────────────────────────┘
                              │ HTTP / REST API
┌─────────────────────────────▼───────────────────────────────────┐
│                    后端服务（Flask）                             │
│  ┌──────────────┐ ┌──────────────┐ ┌──────────────┐            │
│  │  数据预处理   │ │  模型加载    │ │  预测推理    │            │
│  │  (特征工程)   │ │  (加载.pkl)  │ │  (/predict)  │            │
│  └──────────────┘ └──────────────┘ └──────────────┘            │
│  ┌──────────────┐ ┌──────────────┐ ┌──────────────┐            │
│  │  遗传算法    │ │  规则引擎    │ │  历史记录    │            │
│  │  超参数优化   │ │  (健康评估)  │ │  (/history)  │            │
│  └──────────────┘ └──────────────┘ └──────────────┘            │
└─────────────────────────────┬───────────────────────────────────┘
                              │
        ┌─────────────────────┼─────────────────────┐
        ▼                     ▼                     ▼
┌───────────────┐   ┌───────────────┐   ┌───────────────┐
│   SQLite      │   │  训练好的     │   │   上传的      │
│   (存储历史   │   │  模型文件     │   │   数据集      │
│   预测记录)   │   │  (.pkl)      │   │   (.csv)     │
└───────────────┘   └───────────────┘   └───────────────┘