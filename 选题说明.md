# 选题说明

## 一、拟定题目

**面向设备预测性维护的智能分析与预警系统**

## 二、完成目标

本项目旨在开发一套完整的 B/S 架构智能分析与预警系统，面向制造车间设备运维场景，具体目标如下：

1. **故障预测**：基于设备运行参数（空气温度、工艺温度、转速、扭矩、刀具磨损等），利用随机森林分类模型预测设备是否会发生故障，并识别具体的故障类型（刀具磨损故障、散热失效故障、功率失效故障、过载失效故障、随机失效故障）。
2. **超参数优化**：采用遗传算法对随机森林模型的超参数（如决策树数量、最大深度、最小分裂样本数等）进行自动寻优，提升模型预测精度与泛化能力。
3. **健康度评估**：构建设备健康度规则引擎，基于领域知识输出设备健康评分和可解释运维建议，实现“数据驱动 + 规则驱动”的融合决策。
4. **系统落地**：开发具有完整 B/S 架构的可运行 Demo，涵盖前端 UI、后端 API 服务、SQLite 数据库及算法模块，实现从数据输入到结果展示的全流程闭环。

## 三、涉及的技术方向（覆盖 ≥ 3 个课程专题）

| 序号 | 课程专题 | 在本项目中的应用 |
|:---:|---------|---------------|
| 1 | **智能优化技术** | 使用**遗传算法（Genetic Algorithm, GA）** 对随机森林模型的超参数（`n_estimators`、`max_depth`、`min_samples_split` 等）进行全局自动寻优，替代手动调参或网格搜索，提升模型性能。 |
| 2 | **模式识别** | 运用**随机森林（Random Forest）** 分类算法对设备运行模式进行识别，区分“正常”与“故障”状态，并进一步识别五种具体故障类型（TWF / HDF / PWF / OSF / RNF）。 |
| 3 | **智能预测 / 知识工程** | 构建设备**健康度规则引擎**，基于领域知识（温差阈值、功率范围、应变极限等）计算健康评分，结合模型预测结果输出运维建议，实现数据驱动与规则驱动的融合决策。 |